<!DOCTYPE html>
<html>
  <head>
    <title>Docs</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="h-screen bg-gray-50 flex">
    <aside class="border w-2/12 p-5">
      <h1 class="text-2xl text-center">Docs</h1>
      <div class="flex items-center text-sm mt-2">
        <%= component 'avatar', email: current_user.email %>
        <div class="">
          <span class="ml-2"><%= current_user.email %></span>
          <div class="text-center text-gray-600">
            <%= link_to 'Sign out', destroy_user_session_path, data: { turbo_method: :delete } %>
          </div>
        </div>
      </div>
      <hr class="my-3 -mx-5" />
    </aside>

    <main class="pt-5 px-5 h-screen flex-grow overflow-y-auto">
      <%= component 'flash', type: :notice, content: notice %>
      <%= component 'flash', type: :alert, content: alert %>

      <%= yield %>
    </main>
  </body>
</html>
