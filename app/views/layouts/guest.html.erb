<!DOCTYPE html>
<html>
  <head>
    <title>Docs</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="h-screen bg-gray-50">
    <main class="h-full container mx-auto px-5 flex flex-col items-center justify-center">
      <%= component 'flash', content: notice %>
      <%= component 'flash', content: alert, type: :alert %>

      <%= yield %>
    </main>
  </body>
</html>
